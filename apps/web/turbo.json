{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["build-docs.mjs", "docs.css", "../../CHANGELOG.md", "../../docs/feature-specs/*.md"],
      "outputs": ["docs/**"]
    }
  }
}
