{
  "name": "@engine/crawler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "engine-crawl": "./dist/cli.js",
    "engine-crawl-queue": "./dist/queueRunner.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@engine/diagnosis": "workspace:*",
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
