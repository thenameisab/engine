{
  "name": "engine",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:migrate": "turbo run build --filter=@engine/db && node packages/db/dist/cli.js migrate",
    "db:status": "turbo run build --filter=@engine/db && node packages/db/dist/cli.js status",
    "db:user": "turbo run build --filter=@engine/db && node packages/db/dist/cli.js user"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
